@model LoginUser
@{
    Layout = "_MainLayout";
    string msg = ViewData["Message"] as string;
}

<form class="form-horizontal" method="post">
    @if (ViewData["Message"] != null)
    {
        <div class="form-group row">
            <div class="alert alert-@ViewData["MsgType"]">
                <text>@ViewData["Message"]</text>
            </div>
        </div>
    }
    <h2 class="form-signin-heading">Please Sign In</h2>
    <div class="col-5">
        <div class="form-group row">
            <label asp-for="Login_name" class="control-label">Username :</label>
            <input asp-for="Login_name" class="form-control" placeholder="Your login username" />
            <div class="col-3">
                <span asp-validation-for="Login_name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="Password" class="control-label">Password :</label>
            <input asp-for="Password" class="form-control" placeholder="Password" />
            <div class="col-3">
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <input type="submit" class="btn btn-lg btn-primary btn-block" value="Sign In" />
        </div>
    </div>
</form>
